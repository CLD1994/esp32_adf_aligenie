#
# Main Makefile. This is basically the same as a component makefile.
#

COMPONENT_ADD_INCLUDEDIRS := include
COMPONENT_ADD_INCLUDEDIRS += ../audio_codec/mp3_decoder/include
COMPONENT_ADD_INCLUDEDIRS += ../audio_codec/aac_decoder/include
COMPONENT_ADD_INCLUDEDIRS += ../system_init

COMPONENT_SRCDIRS := .
